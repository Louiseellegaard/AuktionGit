name: Docker Image CI for BudAPI

on:
  push:
    branches: [ "develop" ]
    paths: [ "Bud Service/BudService.API/**" ]
  pull_request:
    branches: [ "develop" ]
    paths: [ "Bud Service/BudService.API/**" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Login to Docker Hub 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }} 
      run: 
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        
        
    - name: Build the Docker image for BudAPI 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
      run: docker build . --file "Bud Service/BudService.API/Dockerfile" --tag ${DOCKER_USER}/budapi:latest-dev
      
    - name: Push Docker image to Docker Hub 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
      run: docker push ${DOCKER_USER}/budapi:latest-dev
