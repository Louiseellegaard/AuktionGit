name: Docker Image CI for BrugerAPI

on:
  push:
    branches: [ "develop" ]
    paths: [ "Bruger Service/BrugerService.API/**" ]
  pull_request:
    branches: [ "develop" ]
    paths: [ "Bruger Service/BrugerService.API/**" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Login to Docker Hub 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }} 
      run: 
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        
        
    - name: Build the Docker image for BrugerAPI 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
      run: docker build . --file "Bruger Service/BrugerService.API/Dockerfile" --tag ${DOCKER_USER}/brugerapi:latest-dev
      
    - name: Push Docker image to Docker Hub 
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }} 
      run: docker push ${DOCKER_USER}/brugerapi:latest-dev
